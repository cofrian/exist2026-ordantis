# Core deep-learning stack
torch>=2.1
torchvision>=0.16
transformers>=4.40
sentencepiece            # required by the XLM-RoBERTa / Longformer tokenizers

# Scientific stack
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
Pillow>=10.0

# Official EXIST evaluation framework (ICM / ICM-Soft)
PyEvALL

# Offline Gemini precomputation (only needed to regenerate the Gemini cache)
google-genai>=1.0
python-dotenv>=1.0

# Optional: FlashAttention-2 (falls back to sdpa automatically if absent)
# flash-attn --no-build-isolation
